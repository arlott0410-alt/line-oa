/** Stub สำหรับ build แบบ copy-paste Dashboard (ไม่มี WASM) - ไม่บีบอัดรูป */
export function compressImage(
  _inputBytes: Uint8Array,
  _mimeType: string
): { bytes: Uint8Array; contentType: string; ext: string } | null {
  return null;
}
